# Shared knobs for the test stack — compose interpolates these.
# Change here, `docker compose up` again, no file editing elsewhere.

PROJECT=koopin
REDIS_PASSWORD=supersecret

# Minecraft backend version (itzg/minecraft-server VERSION)
MC_VERSION=1.21.4
PAPER_MEMORY=1G

# Public ports on your host
VELOCITY_PORT=25565
BUNGEE_PORT=25566
